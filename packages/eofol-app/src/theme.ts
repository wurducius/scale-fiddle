const primary = "fuchsia";
const primaryLighter = "hsl(300, 100%, 65%);";
const primaryDarker = "hsl(300, 100%, 25%);";

const secondary = "hsl(180, 100%, 50%)";
const secondaryDarker = "hsl(180, 100%, 35%)";
const secondaryLighter = "hsl(180, 100%, 65%)";

const secondaryDark = "darkcyan";

const font = "fuchsia";

const background = "rgba(0, 0, 0, 0.9)";
const backgroundElevation = "#888888";

export const theme = {
  primary,
  primaryDarker,
  primaryLighter,
  secondary,
  secondaryDark,
  secondaryDarker,
  secondaryLighter,
  font,
  background,
  backgroundElevation,
};
